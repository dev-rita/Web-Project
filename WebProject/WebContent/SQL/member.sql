create table MEMBER(
	memberID varchar(20),
	memberPassword varchar(20),
	memeberName varchar(20),
	memberGender varchar(20),
	memberEmail varchar(50),
	primary key(memberID)
);

select * from MEMBER;
drop table MEMBER;
